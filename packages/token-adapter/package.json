{
  "name": "@icpswap/token-adapter",
  "version": "0.0.1",
  "sideEffects": [
    "*.css.ts"
  ],
  "private": true,
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --pretty --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@icpswap/types": "workspace:*",
    "@icpswap/actor": "workspace:*",
    "@icpswap/utils": "workspace:*",
    "@icpswap/candid": "workspace:*"
  }
}
