{
  "name": "@icpswap/candid",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --pretty --noEmit",
    "clean": "rm -rf node_modules && rm -rf dist",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^13.13.5"
  },
  "dependencies": {
    "@icpswap/dfinity": "workspace:*"
  }
}
