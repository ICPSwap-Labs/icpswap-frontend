{
  "name": "@icpswap/actor",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --pretty --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf build",
    "dev": "tsc --watch",
    "test": "jest --passWithNoTests"
  },
  "devDependencies": {
    "@icpswap/candid": "workspace:*",
    "@icpswap/constants": "workspace:*",
    "@icpswap/types": "workspace:*",
    "@types/node": "^13.13.5",
    "@types/react": "17.0.2",
    "@types/react-dom": "17.0.2"
  },
  "dependencies": {
    "@slide-computer/signer": "^1.7.0",
    "@slide-computer/signer-agent": "^1.7.0"
  }
}
